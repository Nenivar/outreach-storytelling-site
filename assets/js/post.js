/* this is pretty horrible */
/* initialY = 48;
sidebar = document.getElementsByClassName('sidebar');

window.addEventListener('scroll', function(e) {
    Array.prototype.forEach.call(sidebar, function(ele) {
        if (window.scrollY > initialY) {
            ele.setAttribute("style", "position: fixed; top: 0; height: 100%")
        }
    });
}); */